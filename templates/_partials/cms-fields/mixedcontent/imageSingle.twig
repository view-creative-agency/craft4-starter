{% import '_macros/helpers' as helpers %}
{% import '_macros/images' as imageHelper %}

<div
	class="mixedContent_imageSingle"
	id="block-{{ block.id }}"
	data-reveal-on-scroll
>
	{% if block.uncropped | length %}
		{#
			{% set jpgs  = craft.imagerx.transformImage( block.uncropped[0], 'imageSingleUncroppedJpg') %}
			{% set webps = craft.imagerx.transformImage( block.uncropped[0], 'imageSingleUncroppedWebp') %}
			<picture>
				<!-- prefer webp, list these first. Order matters, first media to match is what's rendered - opposite of CSS rules -->
				<source
					media="(max-width: 719px)"
					srcset="{{ webps[0].url }}"
					type="{{ webps[0].mimeType }}"
					width="{{ webps[0].width }}"
					height="{{ webps[0].height }}">
				<source
					media="(min-width: 1280px)"
					srcset="{{ webps[2].url }}"
					type="{{ webps[2].mimeType }}"
					width="{{ webps[2].width }}"
					height="{{ webps[2].height }}">
				<source
					media="(min-width: 720px)"
					srcset="{{ webps[1].url }}"
					type="{{ webps[1].mimeType }}"
					width="{{ webps[1].width }}"
					height="{{ webps[1].height }}">


				<!-- jpg fallbacks -->
				<source
					media="(max-width: 719px)"
					srcset="{{ jpgs[0].url }}"
					type="{{ jpgs[0].mimeType }}"
					width="{{ jpgs[0].width }}"
					height="{{ jpgs[0].height }}">
				<source
					media="(min-width: 1280px)"
					srcset="{{ jpgs[2].url }}"
					type="{{ jpgs[2].mimeType }}"
					width="{{ jpgs[2].width }}"
					height="{{ jpgs[2].height }}">
				<source
					media="(min-width: 720px)"
					srcset="{{ jpgs[1].url }}"
					type="{{ jpgs[1].mimeType }}"
					width="{{ jpgs[1].width }}"
					height="{{ jpgs[1].height }}">

				<img
					loading="lazy"
					decoding="async"
					src="{{ jpgs[0].url }}"
					width="{{ jpgs[0].width }}"
					height="{{ jpgs[0].height }}"
					alt="{{ block.uncropped[0].alt }}"
				/>
			</picture>
		#}

		{{ imageHelper.basic({
			transform: 'imageSingleUncropped',
			source: block.uncropped[0]
		}) }}
	{% elseif block.landscape32 | length %}
		{#
			{% set jpgs  = craft.imagerx.transformImage( block.landscape32[0], 'imageSingleLandscape32Jpg') %}
			{% set webps = craft.imagerx.transformImage( block.landscape32[0], 'imageSingleLandscape32Webp') %}
			<picture>
				<!-- prefer webp, list these first. Order matters, first media to match is what's rendered - opposite of CSS rules -->
				<source
					media="(max-width: 719px)"
					srcset="{{ webps[0].url }}"
					type="{{ webps[0].mimeType }}"
					width="{{ webps[0].width }}"
					height="{{ webps[0].height }}">
				<source
					media="(min-width: 1280px)"
					srcset="{{ webps[2].url }}"
					type="{{ webps[2].mimeType }}"
					width="{{ webps[2].width }}"
					height="{{ webps[2].height }}">
				<source
					media="(min-width: 720px)"
					srcset="{{ webps[1].url }}"
					type="{{ webps[1].mimeType }}"
					width="{{ webps[1].width }}"
					height="{{ webps[1].height }}">

				<!-- jpg fallbacks -->
				<source
					media="(max-width: 719px)"
					srcset="{{ jpgs[0].url }}"
					type="{{ jpgs[0].mimeType }}"
					width="{{ jpgs[0].width }}"
					height="{{ jpgs[0].height }}">
				<source
					media="(min-width: 1280px)"
					srcset="{{ jpgs[2].url }}"
					type="{{ jpgs[2].mimeType }}"
					width="{{ jpgs[2].width }}"
					height="{{ jpgs[2].height }}">
				<source
					media="(min-width: 720px)"
					srcset="{{ jpgs[1].url }}"
					type="{{ jpgs[1].mimeType }}"
					width="{{ jpgs[1].width }}"
					height="{{ jpgs[1].height }}">

				<img
					loading="lazy"
					decoding="async"
					src="{{ jpgs[0].url }}"
					width="{{ jpgs[0].width }}"
					height="{{ jpgs[0].height }}"
					alt="{{ block.landscape32[0].alt }}"
				/>
			</picture>
		#}

		{{ imageHelper.basic({
			transform: 'imageSingleLandscape32',
			source: block.landscape32[0]
		}) }}
	{% elseif block.landscape169 | length %}
		{#
			{% set jpgs  = craft.imagerx.transformImage( block.landscape169[0], 'imageSingleLandscape169Jpg') %}
			{% set webps = craft.imagerx.transformImage( block.landscape169[0], 'imageSingleLandscape169Webp') %}
			<picture>
				<!-- prefer webp, list these first. Order matters, first media to match is what's rendered - opposite of CSS rules -->
				<source
					media="(max-width: 719px)"
					srcset="{{ webps[0].url }}"
					type="{{ webps[0].mimeType }}"
					width="{{ webps[0].width }}"
					height="{{ webps[0].height }}">
				<source
					media="(min-width: 1280px)"
					srcset="{{ webps[2].url }}"
					type="{{ webps[2].mimeType }}"
					width="{{ webps[2].width }}"
					height="{{ webps[2].height }}">
				<source
					media="(min-width: 720px)"
					srcset="{{ webps[1].url }}"
					type="{{ webps[1].mimeType }}"
					width="{{ webps[1].width }}"
					height="{{ webps[1].height }}">

				<!-- jpg fallbacks -->
				<source
					media="(max-width: 719px)"
					srcset="{{ jpgs[0].url }}"
					type="{{ jpgs[0].mimeType }}"
					width="{{ jpgs[0].width }}"
					height="{{ jpgs[0].height }}">
				<source
					media="(min-width: 1280px)"
					srcset="{{ jpgs[2].url }}"
					type="{{ jpgs[2].mimeType }}"
					width="{{ jpgs[2].width }}"
					height="{{ jpgs[2].height }}">
				<source
					media="(min-width: 720px)"
					srcset="{{ jpgs[1].url }}"
					type="{{ jpgs[1].mimeType }}"
					width="{{ jpgs[1].width }}"
					height="{{ jpgs[1].height }}">

				<img
					loading="lazy"
					decoding="async"
					src="{{ jpgs[0].url }}"
					width="{{ jpgs[0].width }}"
					height="{{ jpgs[0].height }}"
					alt="{{ block.landscape169[0].alt }}"
				/>
			</picture>
		#}

		{{ imageHelper.basic({
			transform: 'imageSingleLandscape169',
			source: block.landscape169[0]
		}) }}
	{% elseif block.landscape2391 | length %}
		{#
			{% set jpgs  = craft.imagerx.transformImage( block.landscape2391[0], 'imageSingleLandscape2391Jpg') %}
			{% set webps = craft.imagerx.transformImage( block.landscape2391[0], 'imageSingleLandscape2391Webp') %}
			<picture>
				<!-- prefer webp, list these first. Order matters, first media to match is what's rendered - opposite of CSS rules -->
				<source
					media="(max-width: 719px)"
					srcset="{{ webps[0].url }}"
					type="{{ webps[0].mimeType }}"
					width="{{ webps[0].width }}"
					height="{{ webps[0].height }}">
				<source
					media="(min-width: 1280px)"
					srcset="{{ webps[2].url }}"
					type="{{ webps[2].mimeType }}"
					width="{{ webps[2].width }}"
					height="{{ webps[2].height }}">
				<source
					media="(min-width: 720px)"
					srcset="{{ webps[1].url }}"
					type="{{ webps[1].mimeType }}"
					width="{{ webps[1].width }}"
					height="{{ webps[1].height }}">

				<!-- jpg fallbacks -->
				<source
					media="(max-width: 719px)"
					srcset="{{ jpgs[0].url }}"
					type="{{ jpgs[0].mimeType }}"
					width="{{ jpgs[0].width }}"
					height="{{ jpgs[0].height }}">
				<source
					media="(min-width: 1280px)"
					srcset="{{ jpgs[2].url }}"
					type="{{ jpgs[2].mimeType }}"
					width="{{ jpgs[2].width }}"
					height="{{ jpgs[2].height }}">
				<source
					media="(min-width: 720px)"
					srcset="{{ jpgs[1].url }}"
					type="{{ jpgs[1].mimeType }}"
					width="{{ jpgs[1].width }}"
					height="{{ jpgs[1].height }}">

				<img
					loading="lazy"
					decoding="async"
					src="{{ jpgs[0].url }}"
					width="{{ jpgs[0].width }}"
					height="{{ jpgs[0].height }}"
					alt="{{ block.landscape2391[0].alt }}"
				/>
			</picture>
		#}

		{{ imageHelper.basic({
			transform: 'imageSingleLandscape2391',
			source: block.landscape2391[0]
		}) }}
	{% endif %}
</div>
